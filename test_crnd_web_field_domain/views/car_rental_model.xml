<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="car_rental_model_view_form" model="ir.ui.view">
        <field name="model">car.rental.model</field>
        <field name="arch" type="xml">
            <form string="car_rental_model_form">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="brand_id"/>
                        <label for="car_ids"/>
                        <field name="car_ids" widget="one2many" readonly="1" nolabel="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="car_rental_model_view_tree" model="ir.ui.view">
        <field name="model">car.rental.model</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="brand_id"/>
                <field name="car_ids" widget="one2many_list"/>
            </tree>
        </field>
    </record>

    <record id="car_rental_model_action" model="ir.actions.act_window">
        <field name="name">Model</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">car.rental.model</field>
        <field name="view_mode">tree,form</field>
    </record>

    <menuitem id="car_rental_model_menu"
              name="Model"
              parent="test_crnd_web_field_domain.root"
              action="test_crnd_web_field_domain.car_rental_model_action"
              sequence="50"/>

</odoo>